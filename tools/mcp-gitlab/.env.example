# Credenciales para el MCP GitLab.
# NO edites este archivo: copialo como `.env` y completa con TUS valores.
# En el flujo normal, el server lee estas variables desde el `.env`
# de la raíz del proyecto (load_dotenv) — NO se versionan.
GITLAB_URL=https://gitlab.sreasons.com
GITLAB_TOKEN=tu_token_personal
